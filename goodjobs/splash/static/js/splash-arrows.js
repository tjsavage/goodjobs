var colors = {'blue': '#77DDED'}

var stage = new Kinetic.Stage({
    container: 'arrows-container',
    width: 578,
    height: 363
});

function generateRandomBranch(root, max_angle, splits, split_distance) {
    var points = new Array();
    var prev_point = root;
    points.push(prev_point);
    for (int i = 0; i < splits; i++) {
        
    }
}

var bluePath = new KineticSpline({
    points: [{
        x: 
    }]
})

var layer = new Kinetic.Layer();

var width = stage.getWidth();
var height = stage.getHeight();

layer.add(arrowhead);
stage.add(layer);